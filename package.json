{
  "name": "antifraud",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build:root && npm run build --workspaces --if-present",
    "build:root": "tsc -p tsconfig.json",
    "scenarios:validate": "npm run build:root && node scripts/validate-scenarios.cjs"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.3"
  },
  "private": true
}
